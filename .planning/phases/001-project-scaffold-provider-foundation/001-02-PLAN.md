---
phase: 1
plan: 2
title: "Add static preview assets and shared validation helpers"
wave: 2
type: "auto"
requirements_addressed:
  - PREV-01
  - PREV-02
  - PREV-04
  - API-03
  - CONF-01
  - CONF-02
  - CONF-03
depends_on:
  - "001-01-PLAN.md"
files:
  - public/previews/
  - scripts/
  - lib/tts-validation.ts
  - lib/cost.ts
  - config/
---

<task type="auto">
  <name>Stage local voice previews and validation utilities</name>
  <objective>Make local preview assets/config real and create validation helpers the API route can use.</objective>
  <context>
    This plan depends on config/types from 001-01. Voice previews must be static local files only.
  </context>
  <steps>
    <step id="1">Create `public/previews/`.</step>
    <step id="2">Add a script or documented command sequence for downloading the five seed WAV files from official Google Cloud demo URLs.</step>
    <step id="3">Map downloaded files to `Kore.wav`, `Puck.wav`, `Charon.wav`, `Zephyr.wav`, and `Aoede.wav`.</step>
    <step id="4">If network download fails, create `docs/preview-assets.md` documenting the exact URLs, expected local names, and blocker. Do not fake audio files.</step>
    <step id="5">Ensure `voice.previewUrl` values point to `/previews/<Voice>.wav`.</step>
    <step id="6">Create `lib/tts-validation.ts` with pure validation helpers for request body, voice IDs, language codes, output format, prompt length, mode, and speaker aliases.</step>
    <step id="7">Create `lib/cost.ts` with character count and estimated cost helpers using `COST_PER_1K_CHARS`.</step>
    <step id="8">Add unit-testable exported functions even if tests land later.</step>
  </steps>
  <must_haves>
    <truth>Do not create runtime preview fetch logic.</truth>
    <truth>Do not add preview API route.</truth>
    <truth>Do not commit external URLs as active runtime preview URLs.</truth>
    <truth>Do not transcode to MP3 in V1 unless WAV playback fails.</truth>
  </must_haves>
  <verify>
    <command>Test-Path public/previews</command>
    <command>Get-ChildItem public/previews</command>
    <check>`app/api/tts/preview/route.ts` does not exist.</check>
    <check>`lib/tts-validation.ts` exports validation helpers used by the next plan.</check>
    <check>`lib/cost.ts` computes `chars * 0.15 / 1000`.</check>
    <command>npm run typecheck</command>
  </verify>
  <done>Preview assets are locally staged or explicitly documented as blocked, and validation helpers are ready for the API route.</done>
</task>
