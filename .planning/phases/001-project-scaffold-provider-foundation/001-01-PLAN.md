---
phase: 1
plan: 1
title: "Scaffold app, install dependencies, add base config"
wave: 1
type: "auto"
requirements_addressed:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
depends_on: []
files:
  - package.json
  - tsconfig.json
  - next.config.*
  - tailwind.config.*
  - postcss.config.*
  - app/
  - config/
  - types/
  - .env.example
---

<task type="auto">
  <name>Scaffold ThreeZinc Audio Studio base app</name>
  <objective>Create a runnable Next.js App Router project with typed config files and no provider route yet.</objective>
  <context>
    Read `.planning/phases/001-project-scaffold-provider-foundation/001-CONTEXT.md`,
    `.planning/REQUIREMENTS.md`, `AUDIO_APP_PRD.md`, and `threezinc_design_system (1).md`.
    Preserve existing planning docs.
  </context>
  <steps>
    <step id="1">If no Next.js scaffold exists, create one. If `create-next-app` cannot run in the non-empty root, scaffold into a temporary directory and copy app files back carefully.</step>
    <step id="2">Install Phase 1 dependencies: `@fal-ai/client`, `framer-motion`, `lucide-react`, and required shadcn/ui setup if feasible.</step>
    <step id="3">Ensure `package.json` has `dev`, `build`, `lint`, and `typecheck` scripts.</step>
    <step id="4">Enable TypeScript strict mode.</step>
    <step id="5">Add `.env.example` with `FAL_KEY=your_fal_api_key_here`.</step>
    <step id="6">Add ThreeZinc theme tokens in Tailwind/global CSS: primary, hover, accent, gradient, radius, dark/light surfaces.</step>
    <step id="7">Create `types/tts.ts` with request, response, error, voice, language, speaker, generation, and studio state types.</step>
    <step id="8">Create `config/voices.ts` with MVP voices only: Kore, Puck, Charon, Zephyr, Aoede.</step>
    <step id="9">Create `config/languages.ts` with Auto-detect, English (US), Hindi (India).</step>
    <step id="10">Create `config/expressive-tags.ts` with the canonical MVP tag set.</step>
    <step id="11">Create `config/pricing.ts` with `COST_PER_1K_CHARS = 0.15`.</step>
    <step id="12">Create `config/limits.ts` with prompt max and long-script warning thresholds.</step>
    <step id="13">Keep the root page minimal. It can show a placeholder that Phase 2 will replace with `TTSStudio`.</step>
  </steps>
  <must_haves>
    <truth>No SQLite dependency.</truth>
    <truth>No `/api/tts/preview` route.</truth>
    <truth>No committed secrets.</truth>
    <truth>Use Windows `.cmd` npm/npx shims if PowerShell blocks `.ps1` execution.</truth>
  </must_haves>
  <verify>
    <command>npm run typecheck</command>
    <command>npm run lint</command>
    <command>npm run build</command>
    <check>`config/voices.ts` includes five MVP voices with `previewUrl` fields.</check>
    <check>`.env.example` exists and `.env.local` is not committed.</check>
  </verify>
  <done>Base app runs locally and typed config exists for the provider/UI work that follows.</done>
</task>
